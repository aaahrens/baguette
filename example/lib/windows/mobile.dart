import 'package:flutter/material.dart';

class MobileWindow extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("mobile layout appbar"),
      ),
    );
  }
}
